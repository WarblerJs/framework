# Errors

Public errors include configuration, address, template, encoding, attachment,
transport, and SMTP-specific failures. Credential values and SMTP auth payloads
are never included in error messages.
